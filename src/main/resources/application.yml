## YAML Template.
spring:
  application:
    name: tqueue

  datasource:
    url: jdbc:postgresql://localhost:5432/tqueue
    username: postgres
    password: root
  
  jpa:
    hibernate:
      ddl-auto: update  